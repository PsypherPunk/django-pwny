============
Installation
============

At the command line::

    $ easy_install django-pwny

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-pwny
    $ pip install django-pwny
